sudo: required
services:
  - docker

before_install:
  - docker build -t axteb/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true axteb/docker-react npm run test

deploy:
  provider: elasticbeanstalk
  region: "eu-north-1"
  app: "frontend"
  env: "Frontend-env"
  bucket_name: "elasticbeanstalk-eu-north-1-851725304266"
  bucket_path: "frontend"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: "$AWS_SECRET_KEY"

